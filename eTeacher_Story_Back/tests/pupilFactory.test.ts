import assert = require('assert');
import pupilFactory = require("../src/services/pupilFactory")

describe("Generation Test Suite", () => {
    it("Test A", () => {
        assert.ok(true, "This shouldn't fail");
    });

});
